#TODO: WILL ENABLE IN THE FUTURE

# Component makefile for legacy build system (ESP-IDF v3.x and earlier)

COMPONENT_ADD_INCLUDEDIRS := .
COMPONENT_SRCDIRS := .
COMPONENT_DEPENDS := lwip esp_event espressif__coap